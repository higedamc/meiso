# Miscellaneous
.venv/
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Flutter Rust Bridge generated files (tracked for CI builds)
# NOTE: bridge_generated.dart/ and bridge_generated.h are now tracked
# to avoid requiring LLVM + flutter_rust_bridge_codegen in CI.
# Regenerate locally with: flutter_rust_bridge_codegen generate
rust/bridge_generated.rs

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
/android/local.properties
/android/app/src/

# Rust related
rust/target/
rust/build/
rust/Cargo.lock
rust/target/.rustc_info.json

# External reference projects (not part of main app)
Amber/

# Temporary backup files
.tmp_backup/
pubspec.lock
# rust/src/frb_generated.rs  # tracked for CI builds

# Local utility scripts
migrate_docs_to_anytype.py
start_emulator.sh

scripts/.env
env
