[package]
name = "rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[features]
default = ["tor"]
tor = []

[dependencies]
# Flutter Rust Bridge
flutter_rust_bridge = "=2.12.0"

# Patched fork (see [patch.crates-io]): WebSocket User-Agent for relays (#130)
async-wsocket = { version = "0.10.1", features = ["socks", "tor"] }

# Nostr SDK
nostr-sdk = { version = "0.37", features = ["nip44", "tor"] }

# Async Runtime
tokio = { version = "1.41", features = ["full"] }
futures = "0.3"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error Handling
anyhow = "1.0"
thiserror = "1.0"

# UUID
uuid = { version = "1.11", features = ["v4", "serde"] }

# Date/Time
chrono = { version = "0.4", features = ["serde"] }

# Lazy Static
once_cell = "1.20"

# Cryptography
aes-gcm = "0.10"
argon2 = { version = "0.5", features = ["std"] }
rand = "0.8"
base64 = "0.21"
hex = "0.4"

# MLS (Messaging Layer Security) - Keychat implementation
openmls = { git = "https://github.com/keychat-io/openmls.git", branch = "kc4", package = "openmls"}
openmls_traits = { git = "https://github.com/keychat-io/openmls.git", branch = "kc4", package = "openmls_traits"}
openmls_sqlite_storage = { git = "https://github.com/keychat-io/openmls.git", branch = "kc4", package = "openmls_sqlite_storage"}
kc = { git = "https://github.com/keychat-io/openmls.git", branch = "kc4", package = "kc"}
bincode = "1.3.3"
lazy_static = "1.4.0"

[dev-dependencies]
tempfile = "3.8"

[profile.release]
opt-level = "z"  # 最適化レベル（サイズ優先）
lto = true       # リンク時最適化
codegen-units = 1
strip = true     # シンボル削除
panic = "abort"  # unwind情報を省いてバイナリ縮小

# Meiso-vendored async-wsocket: optional User-Agent on WS handshake (issue #130)
[patch.crates-io]
async-wsocket = { path = "vendor/async-wsocket" }
