name: meiso
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.4.0+379

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.6.1
  
  # Functional Programming
  dartz: ^0.10.1
  
  # Data Models
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  
  # Utils
  uuid: ^4.5.1
  intl: ^0.20.2
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.3.3
  path_provider: ^2.1.5
  
  # Grapheme-cluster truncation (cutting on runes splits emoji apart)
  characters: ^1.4.0

  # Notifications (Phase 3: persistent relay connection + on-device notifications)
  # Pinned to 10.0.0: 11.x requires Flutter >=3.44.0 and this project is on 3.41.6.
  flutter_foreground_task: 10.0.0
  flutter_local_notifications: ^22.3.0
  # Mirror of the shared-group keys the background isolate needs. The Hive
  # `settings` box cannot be opened from a second isolate without corrupting
  # it, so the UI side mirrors the key material here instead. Reading it from
  # a cold headless engine was measured on a physical API 36 device before
  # this dependency was added (RESEARCH/MEISO_PHASE3_SPIKE.md, CHECK-4).
  #
  # 10.x, not 9.x, because of the Android defaults. 9.2.4 defaults to
  # AES/CBC/PKCS7 storage wrapped with RSA/ECB/PKCS1v1.5 — unauthenticated
  # ciphertext and a padding-oracle-prone key cipher, both of which upstream
  # now marks insecure and deprecated. 10.x defaults to AES/GCM/NoPadding
  # wrapped with RSA-OAEP-SHA256. Taking the safe algorithms from the default
  # rather than from an AndroidOptions argument is deliberate: a control that
  # every call site has to remember is a control that one call site will skip.
  flutter_secure_storage: ^10.3.1
  
  # Rust Bridge
  flutter_rust_bridge: 2.12.0
  ffi: ^2.1.0
  
  # UI
  cupertino_icons: ^1.0.8
  table_calendar: ^3.1.2
  google_fonts: ^6.2.1
  
  # Navigation
  go_router: ^14.6.2
  
  # Android Intents (for Amber)
  android_intent_plus: ^5.1.1
  
  # URL Link Preview
  url_launcher: ^6.3.1
  http: ^1.2.2
  html: ^0.15.4
  
  # App Info
  package_info_plus: ^8.1.2
  
  # Lottie Animations
  lottie: ^3.1.3
  
  # Logging
  talker_flutter: ^4.6.1
  
  # Image
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1
  crypto: ^3.0.6
  mobile_scanner: ^7.2.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Test-only: lets the notification settings store test install a
  # platform store that refuses one key.
  shared_preferences_platform_interface: ^2.4.2

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  
  # Stricter lints based on flutter_lints
  pedantic_mono: any
  
  # Testing
  mocktail: ^1.0.4
  
  # Code Generation
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0
  ffigen: ^13.0.0
  
  # App Icon Generation
  flutter_launcher_icons: ^0.14.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  
  # Enable generation of localized messages
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/lottie/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# App Icon Configuration
flutter_launcher_icons:
  android: true
  ios: false  # iOS対応は現時点では不要
  image_path: "assets/icon.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/icon.png"
