<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ネットワークアクセスに必要なパーミッション -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- QR コード読み取り（メンバー追加）に必要なパーミッション -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <!-- Phase 3 notifications: the relay listener runs as a foreground service of type
         "specialUse" (Android 14+). FOREGROUND_SERVICE / WAKE_LOCK / RECEIVE_BOOT_COMPLETED /
         POST_NOTIFICATIONS are declared by the flutter_foreground_task plugin manifest and
         merged in; only the app-specific permissions are declared here. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Lets the settings screen offer the battery-optimization exemption so the relay
         listener is not killed by Doze. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <application
        android:label="meiso"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:localeConfig="@xml/locales_config"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Amberからのレスポンスを受け取るIntent Filter (アプリ専用スキーム) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="meiso"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Phase 3 notifications: persistent relay listener hosted by flutter_foreground_task.
             The plugin manifest declares its boot/restart receivers but NOT the service, so the
             app must declare it. Do not rename android:name; the plugin starts this class.
             "specialUse" is used instead of "dataSync" because dataSync is capped at 6h/24h and
             is on the Android 15 boot-start denylist; specialUse is not (verified on API 36,
             see RESEARCH/MEISO_PHASE3_SPIKE.md). The subtype text is reviewed by Google Play and
             describes what the service actually does. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps a WebSocket connection to the user's Nostr relays open so that new comments on shared tasks are received and shown as notifications without any push server." />
        </service>

        <!-- Monochrome status-bar icon for the listener notification and comment notifications.
             flutter_foreground_task resolves NotificationIcon(metaDataName) through this
             meta-data entry; the default (launcher icon) renders as a white square. -->
        <meta-data
            android:name="jp.godzhigella.meiso.notify.STATUS_ICON"
            android:resource="@drawable/ic_stat_notify" />
        
        <!-- Home Screen Widget -->
        <receiver
            android:name=".TodoWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- ブラウザアプリを開くためのクエリ (url_launcher用) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- Amber (Nostr Signer) パッケージへのクエリ -->
        <package android:name="com.greenart7c3.nostrsigner" />
    </queries>
</manifest>
